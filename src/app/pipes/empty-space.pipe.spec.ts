import { EmptySpacePipe } from './empty-space.pipe';

describe('EmptySpacePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptySpacePipe();
    expect(pipe).toBeTruthy();
  });
});
